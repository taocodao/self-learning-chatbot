{
  "version": 2,
  "buildCommand": "echo 'No build needed'",
  "installCommand": "echo 'No install needed'",
  "rewrites": [
    { "source": "/health", "destination": "/api" },
    { "source": "/webhook", "destination": "/api" },
    { "source": "/api/health", "destination": "/api" },
    { "source": "/api/webhook", "destination": "/api" }
  ],
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 10
    }
  }
}
