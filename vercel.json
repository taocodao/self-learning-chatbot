{
    "version": 2,
    "buildCommand": "cd packages/backend && npm install && npm run build",
    "installCommand": "npm install",
    "outputDirectory": "packages/backend/dist",
    "builds": [
        {
            "src": "packages/backend/dist/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "packages/backend/dist/server.js"
        }
    ]
}